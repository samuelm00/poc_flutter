/// Namespace for Default Dog Owner App Breakpoints
abstract class AppBreakpoints {
  /// Max width for a small layout.
  static const double small = 380;

  /// Max width for a large layout.
  static const double medium = 426;

  /// Max width for a large layout.
  static const double large = 480;
}
